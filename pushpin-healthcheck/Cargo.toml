[package]
name = "pushpin-healthcheck"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = "0.14"
hyperlocal = "0.8"
tokio = { version = "1.0", features = ["io-std", "io-util", "macros", "rt-multi-thread"] }
