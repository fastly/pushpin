/var/log/pushpin/*.log {
        daily
        missingok
        rotate 7
        sharedscripts
        postrotate
                invoke-rc.d --quiet pushpin reload > /dev/null
        endscript
        compress
        notifempty
}
